# Paddysun Newsprint 操作手册

适用版本：0.10.11。建议先在独立测试站安装，备份数据库、媒体和当前主题后再迁移。此手册不包含作者站点的数据库、测试账号或部署凭据。

## 1. 安装与首次启用

1. 确认使用 WordPress 7.1+；实际验证环境为 PHP 8.3.30，建议 PHP 8.3+。
2. 后台「外观 → 主题 → 添加新主题 → 上传主题」，上传独立安装包 `paddy-newsprint-0.10.11.zip`，安装并启用。
3. 如果下载的是 GitHub 仓库 ZIP，先解压，单独上传其中 `paddysun-newsprint/` 至 `wp-content/themes/`。不要把带 README/SECURITY/手册的整个仓库归档当作主题包安装。
4. 到「设置 → 固定链接」保存一次。已有站点保持原有 URL 规则，不要为了换主题任意更改固定链接。
5. 到「外观 → 编辑器」设置导航、站点标题、页脚与配色。已有经典菜单可沿用 `primary` 导航位置；具体入口以当前 WordPress 界面为准。

公开版页脚备案号和服务状态是占位内容，链接为 `#`。上线前替换为自己的合法信息，或删除不需要的项目；不要将示例备案号当作真实备案展示。

## 2. 换装旧站与导入内容

- 更换主题通常不删除文章、评论与媒体。先保存原主题自定义设置，停用旧主题不等于删除其数据库数据。
- WXR 是内容/XML导出文件，不包含附件二进制本身。导入器可能尝试下载附件；附件能否取得取决于源站、网络与导入选项，不保证无损迁移。
- 导入后核对文章/页面/分类/评论及作者映射、特色图、旧链接、附件访问和导航。
- 「首页」「文章页」及自定义模板可能保存在数据库。不要为排查显示差异直接清空所有模板或覆盖文章内容。

## 3. 首页三栏与阅读入口

首页提供近日简报、长读主稿、右侧栏、订阅区与分类要目；近期文章卡片流已删除。桌面（≥1025px）三栏网格总高度预算为 **820px**；中栏和右栏正文使用剩余空间，阅读入口不收缩。手机、平板使用自然高度。

- 需要改桌面总高时，在主题 `style.css` 找 `--np-lead-clip`。直接改主题文件会在升级时被覆盖，长期定制建议放入子主题或额外样式。
- 主稿支持置顶文，简报/侧栏有对应偏移处理；多置顶、分类筛选与定制模板组合仍应在自己的内容上复测。
- 单篇特色图在文章头显示，无特色图时不生成空占位块。右侧栏不显示特色图。
- 完整报头的手机断点为 ≤760px：副标题、日期/星期及刊训隐藏；站点名称、卷期和检索/RSS入口保留。

## 4. 样式与模板副本

配色、字体、间距优先通过站点编辑器「样式」设置。模板与模板部件在编辑器保存后可能形成数据库副本，优先于主题文件；升级后旧副本不会自动同步。

若文件更新未生效：先保留自定义文案，检查是否存在对应模板/部件自定义。只重置需要恢复的那一个，不要直接删除整个报头或页脚。首页完整报头和文章页紧凑报头是不同部件。

关于页人物版式只包含简介与时间线；友链模板、友链网格、徽章墙样板已移除。已有页面中的旧样板引用可能需要手工迁移，用户文章不会随文件删除而自动转换。

## 5. Markdown、公式、图表与表格

- 推荐把 Markdown 内容导入/转换为核心区块；粘贴行为取决于编辑器与原内容格式，不保证任意文档自动、无损转块。
- 编辑文章时可在「原文 MD」字段保留 Markdown 源文；复制/导出优先采用有效源文，否则由服务端 HTML 转换。格式转换存在边界，应核对复杂表格、脚注与自定义 HTML。
- 公式和图表主要在前台增强渲染；编辑器可编辑源码，不承诺全部编辑器画布实时渲染。
- Mermaid 仅放行支持的类型（流程图、时序、类、状态、ER、思维导图、饼图）；其他类型或解析失败显示源码，不应当作渲染器全功能支持。
- 长内容折叠使用「展开/收起」按钮；折叠态按钮覆盖在内容内部下缘。正文图片的折叠与长内容策略应检查窄屏表现。宽表容器支持横向滚动与主题化滚动条。
- 目录通过文章级设置或模板类选择头部、悬浮或关闭；悬浮目录需要足够宽的视口和相应标题结构。

## 6. 图片与视频失效替换

`paddysun-newsprint/assets/artwork-fallbacks.json` 是固定配对数组，每项包含 `image`、`url`、`alt`、`title`。公开版的介绍链接与作品名使用示例值，请上线前改成真实配对。

- 图片字段可指向主题内资产或显式 http/https 地址；介绍链接须为合法地址。协议相对 URL（`//host/...`）拒绝。
- 当前示例图片路径按站点根 `/wp-content/themes/paddysun-newsprint/...` 写入。若 WordPress 安装在子目录、修改了 wp-content 路径或主题目录名，需要同步这些路径；不要假定自动适配。
- 正文图片加载失败或带 `src` 的 video 报错时可替换为作品图与提示；跨域 iframe 播放器无法以此可靠检测，不在范围内。
- 小图（渲染宽高≤64px）、88×31按钮、指定头像类和 `[data-np-keep]` 标记不参与作品替换。替换失败降级文字，不循环重试。
- `assets/img/placeholders/` 是可选内容素材，未使用的素材文件不会自动参与版面。
- 主题升级会覆盖主题内 JSON，请自行备份；本主题没有独立设置 UI 或自动包外配置装载器。

## 7. 提醒确认弹窗

用途为通用提醒，**不是法律同意记录或访问控制门禁**。

1. 在站点编辑器的「图案/模板部件」中找到 **提醒确认弹窗**，兼容 slug 为 `maintenance-notice`。
2. 编辑原件：标题、正文、确认按钮分别是独立核心区块。编辑画布应显示普通卡片，不自动弹出模态窗口。
3. 将部件插入需要的模板、报头或页脚，每页一份。不要在提醒部件内部插入自身，也不要让两个部件循环引用。
4. 前台实际引用时才加载提醒脚本。关闭按钮、Esc、遮罩均可退出；同源当前标签页会话不再提醒，存储不可用时可能再次出现。重复挂载只打开第一个实例。
5. 撤下引用即可停止提醒，不会按日期自动消失。

旧版将 dialog 放在 HTML 区块中，若数据库旧副本仍被使用，先备份文案再重置/迁移**这一部件**；不要清空整个报头/页脚。0.10.11 已增加编辑器隔离与异常保护，但先前浏览器崩溃尚无可复现错误栈，插入、修改、保存应在自己的测试站复测。

## 8. 导出接口与隐私

- `/wp-json/paddysun/v1/posts/<id>/markdown`：单篇 Markdown。
- `/llms.txt`：索引；`/llms-full.txt`：全量正文导出。
- 非公开、密码保护及关闭复制/导出的文章会按代码规则排除。请用匿名与登录状态分别检查自己的站点。
- 修改公开性后，先撤下内容，再清对象缓存与 CDN 缓存。仅清浏览器缓存不能撤回边缘节点已缓存的响应。
- 可选 SEO/缓存/评论/安全插件需要各自配置；主题静态审查与少量测试不能替代完整插件兼容矩阵。

## 9. COS 与其他媒体存储

主题图片错误监听不依赖同源请求；媒体存储换域原则上不需要改渲染脚本。但 COS 插件接入结论目前主要来自静态审查，安装后请测试题图、正文图、404兜底、懒加载、导出链接和 CDN 缓存。文章里手工写死的旧媒体 URL 不会因换主题而自动迁移。

## 10. 升级、校验与回滚

1. 备份数据库、媒体、主题目录和自定义 JSON。
2. 根据下载页 SHA-256 校验对应的**同名安装包**。Windows：`certutil -hashfile paddy-newsprint-0.10.11.zip SHA256`；Linux/macOS：`shasum -a 256 paddy-newsprint-0.10.11.zip`。
3. 上传新主题包并替换旧版，清对象缓存/CDN静态缓存，再硬刷新浏览器。
4. 核对首页、单篇、归档、检索、404、Feed、两个 llms 端点、导航和提醒弹窗。
5. 遇到异常先保留错误信息并回滚已备份的主题文件；不要靠删除文章或数据库表排错。安全报告渠道见 SECURITY.md。

仓库与独立安装包使用相同版本号，但仓库归档包含额外根文档，因此两者哈希不同。不得把自用含个人配置的包的哈希写成公开包哈希。
